Building:
  * To build just the javascript: ./build-system.sh
  * To minimize the javascript & create an ACD (Atomic Client Document): ./atomize.pl

Opening ACD's
  * Local:  Browse to your ACD and open it locally with your web browser

  * Hosted: 1) Copy the ACD (static-client.html) into atomosd's "www" folder
            2) Run the atomosd webserver
            3) Visit the atomosd server on port 5551 (eg. http://localhost:5551/static-client.html)

